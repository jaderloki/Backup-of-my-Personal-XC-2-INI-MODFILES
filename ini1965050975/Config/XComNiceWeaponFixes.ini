;---------------------------------------------------------------------------------------
;  FILE:   XcomXenolator Config - MrNiceWeaponFixes                                 
;  
;	File created by RustyDios	24/11/19	02:45
;	LAST UPDATED				08/06/20	04:00
;
;	Configs - this is used to allow the chemthrowers to burn stuff
;
;*******************************************************************************************

[WepUpgradeFix.X2DownloadableContentInfo_WepUpgradeFix]

; Fields for FixAbility Entries:
; TemplateName: Template name of the ability to fix, duh!
; OnByDefault: Status of fix before MCM configuration, defauls to TRUE
; DisabledByMod: Will be force diabled (and greyed out in MCM) if the named mod is enabled
; AddPrimaryViz: Whether to explicitly add any visualization for ammo/weapon effects for the primary target, defaults to FALSE
; IgnoreExistingDelegates: If set igonores existing BuildNewGameStatFn/BuildVisualizationFn, and uses typical ability delegates instead. defaults to FALSE
; SetFlag: Whether to set the ammo effects and weapon effects flags to true, defaults to TRUE

;FixAbility is to add Ammo effects/bonus damage to multitarget skills
+FixAbility = (TemplateName="RDThrowerShot_Fire")
+FixAbility = (TemplateName="RDThrowerOverwatchShot_Fire")
;+FixAbility = (TemplateName="RDChemstorm_Fire") ;; no longer required due to Mitzruti's new chemstorm code

+FixAbility = (TemplateName="RDThrowerShot_Acid")
+FixAbility = (TemplateName="RDThrowerOverwatchShot_Acid")
+FixAbility = (TemplateName="RDChemstorm_Acid")

+FixAbility = (TemplateName="RDThrowerShot_Poison")
+FixAbility = (TemplateName="RDThrowerOverwatchShot_Poison")
+FixAbility = (TemplateName="RDChemstorm_Poison")

;+FixAbility = (TemplateName="RDChemstormFire");no longer required
+FixAbility = (TemplateName="XENOLATOR_Directed")
+FixAbility = (TemplateName="XENOLATOR_FlameArea")
+FixAbility = (TemplateName="XENOLATOR_ArcCutter")
